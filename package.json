{
  "name": "dropp-site",
  "version": "2.0.0",
  "private": true,
  "description": "Site DROPP — dropp.care. Génération statique multilingue (14 langues), zéro dépendance.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "i18n:check": "node scripts/i18n-check.js",
    "test": "node scripts/i18n-check.js && node scripts/build.js --quiet && node --test tests/*.test.js",
    "serve": "node scripts/serve.js",
    "dev": "node scripts/build.js && node scripts/serve.js"
  }
}
